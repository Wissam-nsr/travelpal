<%= simple_form_for [@trip, @step], url: trip_steps_path do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :date, as: :date%>
  <%= f.input :location %>
  <%= f.input :photo, as: :file %>
  <%= f.button :submit %>
<% end %>
